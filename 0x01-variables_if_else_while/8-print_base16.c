#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 *
 */

int main(void)
{
	char num = '0';
	char let = 'a';

	for (num = '0'; num <= '9'; a++)
	{
		putchar(a);
	}
	for (let = 'a'; let <= 'f'; a++)
	{
		putchar(let);
	}
	putchar('\n');
	return (0);
}
