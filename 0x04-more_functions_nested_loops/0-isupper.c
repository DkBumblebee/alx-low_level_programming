#include "main.h"

/**
 * _isupper - functions that checkes for uppercase character
 * Return 1 if @c is uppercase
 */

int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))
	return (1);

	return (0);
}
